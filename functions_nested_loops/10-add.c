#include "main.h"

/**
 *add - sums two integers
 *@slice1: int1
 *@slice2: int2
 *@slice3 - sum of them slices
 *Return: sum of the integers
 */

int add(int slice1, int slice2)
{
	int slice3 = (slice1 + slice2);
		return (slice3);
}
