#include <stdio.h>

/**
 *main - entry death
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	int base10 = 0;

	for (base10 = 48; base10 < 58; base10++)
		{
		putchar(base10);
		}
	putchar('\n');
	return (0);
}
