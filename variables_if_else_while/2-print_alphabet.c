#include <stdio.h>

/**
*main - war on betty
*
*Return: Always 0 (Success)
*/

int  main(void)
{
	char pizza;

	for (pizza = 'a'; pizza <= 'z'; pizza++)
		putchar(pizza);
	putchar("\n");
	return (0);
}
