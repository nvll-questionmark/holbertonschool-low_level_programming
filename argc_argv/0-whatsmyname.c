#include <stdio.h>

/**
 * main - name of program
 * @argc: number of argument
 * @argv: array of arguments
 *
 * Return: 0 Succes G
 */

in main(int argc __attribute__((unused)), char *argv[])
{
	printf("%s\n", *argv);

	return (0);
}
